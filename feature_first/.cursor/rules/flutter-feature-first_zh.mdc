---
description: 基于 Riverpod 的特性优先架构
globs: *
alwaysApply: true
---

## 概述

本文档概述了基于实际项目分析的当前项目结构和架构模式。遵循这些指南以在应用Flutter最佳实践的同时保持一致性。

## 架构概述

- 基于Riverpod状态管理的Feature-first架构
- 基于Hooks的组件组合
- 领域驱动设计(DDD)的分层架构
  - Domain层：业务逻辑和规则
  - Infrastructure层：数据访问和外部服务
  - Presentation层：用户界面和交互
- Firebase作为后端服务
- SharedPreferences用于本地持久化
- Retrofit用于API通信
- 使用pedantic_mono和custom_lint进行静态分析

## 项目结构

```
lib/
  ├── features/           // 功能模块
  │   ├── account/       // 账户相关功能
  │   │   ├── domain/   // 领域层
  │   │   │   ├── entities/     // 领域实体
  │   │   │   ├── repositories/ // 仓库接口
  │   │   │   └── use_cases/    // 用例（业务逻辑）
  │   │   ├── infrastructure/   // 基础设施层
  │   │   │   └── repositories/ // 仓库实现
  │   │   └── presentation/     // 表现层
  │   │       ├── pages/       // 页面
  │   │       └── widgets/     // 组件
  │   ├── authentication/      // 认证功能
  │   ├── github_users/       // GitHub用户功能
  │   ├── home/              // 主页功能
  │   ├── memo/              // 备忘录功能
  │   ├── setting/           // 设置功能
  │   ├── timeline/          // 时间线功能
  │   └── ...               // 其他功能模块
  ├── core/                  // 核心功能
  │   ├── domain/           // 核心领域层
  │   │   ├── entities/     // 核心领域实体
  │   │   ├── repositories/ // 核心仓库接口
  │   │   └── use_cases/    // 核心业务逻辑
  │   │       ├── authentication/ // 认证相关用例
  │   │       ├── analytics/      // 分析相关用例
  │   │       └── permission/     // 权限相关用例
  │   ├── infrastructure/    // 核心基础设施层
  │   │   └── repositories/  // 核心仓库实现
  │   ├── presentation/      // 核心表现层
  │   │   └── widgets/      // 共享组件
  │   ├── exceptions/        // 自定义异常
  │   ├── extensions/        // 扩展方法
  │   ├── utils/            // 工具函数
  │   ├── router/           // 导航路由
  │   ├── res/              // 资源（生成的）
  │   └── custom_hooks/     // 自定义Flutter hooks
  ├── app.dart              // 应用配置
  └── main.dart             // 入口点
```
## 领域驱动设计（DDD）层次说明

### Domain层
- 包含业务逻辑和规则
- 不依赖于具体的技术实现
- 包括：
  - Entities（实体）：核心业务对象
  - Repositories（仓库接口）：定义数据操作契约
  - Use Cases（用例）：具体业务逻辑实现

### Infrastructure层
- 实现Domain层定义的接口
- 处理外部系统交互
- 包括：
  - Repository实现
  - 外部服务适配器
  - 数据源实现

### Presentation层
- 处理用户界面和交互
- 使用Domain层的Use Cases
- 包括：
  - Pages（页面）
  - Widgets（组件）
  - View Models/Controllers
  
## 包依赖

### 核心依赖

- Flutter SDK (>=3.4.0)
- flutter_localizations
- cupertino_icons

### 状态管理和UI

- hooks_riverpod
- riverpod_annotation
- flutter_hooks
- go_router（导航）

### Firebase服务

- firebase_core
- firebase_analytics
- cloud_firestore
- firebase_auth
- firebase_storage
- cloud_functions
- firebase_messaging
- firebase_crashlytics
- firebase_remote_config

### 数据管理

- shared_preferences
- retrofit
- dio
- freezed_annotation
- json_annotation
- equatable

### UI组件和功能

- pull_to_refresh_flutter3
- extended_image
- flutter_svg
- flutter_slidable
- adaptive_dialog
- page_transition
- flutter_inappwebview
- url_launcher
- map_launcher
- image_picker
- image_cropper
- flutter_local_notifications

### 开发依赖

- pedantic_mono
- custom_lint
- riverpod_lint
- json_serializable
- build_runner
- freezed
- riverpod_generator
- retrofit_generator
- flutter_gen_runner

## 状态管理指南

1. 使用带有@riverpod注解的Riverpod
2. 让riverpod_generator决定适当的Notifier类
3. 使用SharedPreferences进行本地存储
4. 使用Freezed遵循正确的状态不可变模式
5. 使用适当的provider作用域

## 功能组织

1. 每个功能应该在features/目录下自包含
2. 功能结构应包括：
   - pages/（UI界面）
   - widgets/（功能特定组件）
   - providers/（状态管理）
   - repositories/（数据访问）
   - entities/（功能特定实体）

## 核心层指南

1. 核心功能应放置在适当的core/子目录中
2. 共享组件应放置在core/widgets/中
3. 业务逻辑应在core/use_cases/中
4. 领域实体应在core/entities/中
5. 仓库接口应在core/repositories/中

## 代码生成

1. 使用build_runner进行代码生成：
   ```bash
   flutter pub run build_runner build
   ```
2. 为以下内容生成代码：
   - Freezed模型
   - Riverpod providers
   - Retrofit API客户端
   - JSON序列化
   - FlutterGen资源

## 测试指南

1. 将测试放在test目录中
2. 为业务逻辑编写单元测试
3. 为UI组件实现组件测试
4. 使用mockito进行依赖模拟

## 性能指南

1. 使用extended_image实现适当的图片缓存
2. 使用pull_to_refresh_flutter3优化列表视图
3. 遵循适当的状态管理模式
4. 实现适当的Firebase缓存策略

## 命名指南

1. 遵循一致的命名规范：

   - 目录名：snake_case
   - 文件名：snake_case
   - 类名：UpperCamelCase
   - 变量名：camelCase

2. 功能特定命名：

   - 页面：NounPage（例如，HomePage）
   - 组件：NounComponent（例如，UserCard）
   - 模型：NounModel（例如，UserModel）
   - Providers：NounProvider（例如，UserProvider）
   - 仓库：NounRepository（例如，UserRepository）

3. 一般原则：
   - 实体使用名词或形容词
   - 动作使用动词
   - 考虑命名的复数形式
   - 保持名称清晰和描述性

## 静态分析

1. 遵循pedantic_mono lint规则
2. 使用custom_lint进行额外检查
3. 使用riverpod_lint进行Riverpod特定规则检查
4. 修复所有分析警告
5. 使用适当的空安全模式
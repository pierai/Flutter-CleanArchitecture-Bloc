---
description: 基于BLoC的特性优先架构
globs: *
alwaysApply: true
---
## 概述

本文档概述了基于实际项目分析的当前项目结构和架构模式。遵循这些指南以在应用Flutter最佳实践的同时保持一致性。

## 架构概述

- 基于BLoC模式的Feature-first架构
- 基于Hooks的组件组合
- 领域驱动设计(DDD)的分层架构
  - Domain层：业务逻辑和规则
  - Infrastructure层：数据访问和外部服务
  - Presentation层：用户界面和交互
- SharedPreferences用于本地持久化
- Retrofit用于API通信
- 使用flutter_lints和custom_lint进行静态分析

## 项目结构

```
lib/
  ├── features/           // 功能模块
  │   ├── account/       // 账户相关功能
  │   │   ├── domain/   // 领域层
  │   │   │   ├── entities/     // 领域实体
  │   │   │   ├── repositories/ // 仓库接口
  │   │   │   └── use_cases/    // 用例（业务逻辑）
  │   │   ├── infrastructure/   // 基础设施层
  │   │   │   └── repositories/ // 仓库实现
  │   │   └── presentation/     // 表现层
  │   │       ├── pages/       // 页面
  │   │       ├── widgets/     // 组件
  │   │       └── bloc/        // 状态管理。领域层（domain）应该保持 框架无关性，Bloc 的框架依赖性（如 Flutter 的 Stream/Bloc 库）使其更适合放在表现层
  │   │           ├── account_bloc.dart     // BLoC类
  │   │           ├── account_event.dart    // 事件定义
  │   │           └── account_state.dart    // 状态定义
  │   ├── authentication/      // 认证功能
  │   ├── home/              // 主页功能
  │   ├── setting/           // 设置功能
  │   └── ...               // 其他功能模块
  ├── core/                  // 核心功能
  │   └── di/              # 依赖注入配置
  │   │   ├── injector.dart
  │   │   └── modules/     # 按功能划分的注入模块
  │   ├── presentation/      // 核心表现层
  │   │   └── widgets/      // 共享组件
  │   │   └── bloc/         // 全局状态管理           
  │   │       ├── app_bloc.dart  // 应用级BLoC
  │   │       ├── app_event.dart // 应用级事件
  │   │       └── app_state.dart // 应用级状态
  │   ├── exceptions/        // 自定义异常
  │   ├── extensions/        // 扩展方法
  │   ├── utils/            // 工具函数
  │   ├── router/           // 导航路由
  │   ├── res/              // 资源（生成的）
  │   │   ├── colors/
  │   │   ├── strings/
  │   │   ├── themes/
  │   │   └── icons/
  │   └── custom_hooks/     // 自定义Flutter hooks
  └── test/                  # 测试目录与源码平行（可选）
  │   └── features/
  │   │   └── account/
  │   │       ├── domain/
  │   │       └── presentation/
  ├── app.dart              // 应用配置
  └── main.dart             // 入口点
```
## 领域驱动设计（DDD）层次说明

### Domain层
- 包含业务逻辑和规则
- 不依赖于具体的技术实现
- 包括：
  - Entities（实体）：核心业务对象
  - Repositories（仓库接口）：定义数据操作契约
  - Use Cases（用例）：具体业务逻辑实现

### Infrastructure层
- 实现Domain层定义的接口
- 处理外部系统交互
- 包括：
  - Repository实现
  - 外部服务适配器
  - 数据源实现

### Presentation层
- 处理用户界面和交互
- 使用Domain层的Use Cases
- 包括：
  - Pages（页面）
  - Widgets（组件）
  - View Models/Controllers
  

## 包依赖

### 架构和状态管理 🏗️
- `flutter_bloc: ^8.1.4` - Bloc 状态管理库，用于分离业务逻辑和 UI
- `bloc` - Bloc 核心实现
- `bloc_concurrency` - Bloc 并发控制
- `get_it: ^7.6.7` - 依赖注入容器
- `injectable: ^2.3.2` - 简化依赖注入配置

### 网络和数据处理 🌐
- `dio: ^5.4.0` - HTTP 客户端
- `retrofit` - REST API 客户端生成器
- `objectbox: ^2.5.0` - 本地数据库，高性能 NoSQL
- `shared_preferences` - 键值对存储

### UI 和交互 📱
- `auto_route: ^7.8.4` - 路由管理
- `flutter_screenutil: ^5.9.0` - 屏幕适配
- `pull_to_refresh_flutter3` - 下拉刷新
- `infinite_scroll_pagination: ^4.0.0` - 无限滚动分页
- `flutter_slidable` - 滑动操作组件
- `dismissible_page` - 可滑动关闭的页面
- `adaptive_dialog` - 自适应对话框
- `page_transition` - 页面转场动画
- `flutter_inappwebview` - 内置浏览器

### 图片处理 🖼️
- `flutter_svg: ^2.0.9` - SVG 图像支持
- `cached_network_image: ^3.3.1` - 图片缓存
- `extended_image` - 高级图片处理
- `image_picker` - 图片选择器
- `image_cropper` - 图片裁剪

### 实用工具 🛠️
- `dartx: ^1.2.0` - Dart 扩展方法集合
- `rxdart: ^0.27.7` - 响应式编程扩展
- `intl: ^0.18.1` - 国际化支持
- `collection` - Dart 集合操作工具
- `tuple: ^2.0.2` - 元组数据类型
- `uuid` - UUID 生成器
- `quiver` - 实用工具库
- `logger` - 日志工具

### 平台集成 📱
- `path_provider` - 文件系统路径
- `device_info_plus` - 设备信息
- `url_launcher` - URL 处理
- `map_launcher` - 地图应用启动
- `flutter_local_notifications` - 本地通知
- `flutter_udid` - 设备唯一标识

### 代码生成 ⚙️
- `freezed_annotation` - 不可变对象注解
- `json_serializable` - JSON 序列化生成
- `json_annotation` - JSON 序列化注解
- `equatable` - 相等性比较
- `build_runner` - 代码生成工具
- `freezed` - 不可变类生成器
- `retrofit_generator` - Retrofit 客户端生成器
- `flutter_gen_runner` - 资源代码生成器
- `injectable_generator` - 依赖注入代码生成器

### 开发和测试 🔧
- `flutter_lints: ^3.0.1` - Flutter 代码规范
- `custom_lint` - 自定义代码检查
- `pedantic_mono` - 严格代码风格检查
- `mocktail: ^1.0.3` - 模拟测试框架
- `bloc_test: ^9.1.6` - Bloc 测试工具
- `melos: ^4.1.0` - 多模块项目管理


## 状态管理指南

1. BLoC模式实现原则：
   - 使用Event作为输入
   - 使用State作为输出
   - 使用emit发送新状态
   - 实现clear separation of concerns

2. BLoC类实现规范：
```dart
class AccountBloc extends Bloc<AccountEvent, AccountState> {
  AccountBloc(this._accountRepository) : super(const AccountState.initial()) {
    on<AccountLoaded>(_onAccountLoaded);
    on<AccountUpdated>(_onAccountUpdated);
  }

  final AccountRepository _accountRepository;

  Future<void> _onAccountLoaded(
    AccountLoaded event,
    Emitter<AccountState> emit,
  ) async {
    emit(const AccountState.loading());
    try {
      final account = await _accountRepository.getAccount();
      emit(AccountState.loaded(account));
    } catch (error) {
      emit(AccountState.error(error.toString()));
    }
  }
}
```

3. Event定义规范：
```dart
abstract class AccountEvent extends Equatable {
  const AccountEvent();

  @override
  List<Object?> get props => [];
}

class AccountLoaded extends AccountEvent {
  const AccountLoaded();
}

class AccountUpdated extends AccountEvent {
  const AccountUpdated(this.account);

  final Account account;

  @override
  List<Object?> get props => [account];
}
```

4. State定义规范：
```dart
@freezed
class AccountState with _$AccountState {
  const factory AccountState.initial() = _Initial;
  const factory AccountState.loading() = _Loading;
  const factory AccountState.loaded(Account account) = _Loaded;
  const factory AccountState.error(String message) = _Error;
}
```

5. UI集成规范：
```dart
class AccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (context) => AccountBloc(
        context.read<AccountRepository>(),
      )..add(const AccountLoaded()),
      child: BlocBuilder<AccountBloc, AccountState>(
        builder: (context, state) {
          return state.when(
            initial: () => const SizedBox(),
            loading: () => const CircularProgressIndicator(),
            loaded: (account) => AccountView(account: account),
            error: (message) => ErrorView(message: message),
          );
        },
      ),
    );
  }
}
```

## 功能组织

1. 每个功能应该在features/目录下自包含
2. 功能结构应包括：
   - pages/（UI界面）
   - widgets/（功能特定组件）
   - bloc/（状态管理）
   - repositories/（数据访问）
   - entities/（功能特定实体）

## 核心层指南

1. 核心功能应放置在适当的core/子目录中
2. 共享组件应放置在core/widgets/中
3. 业务逻辑应在core/use_cases/中
4. 领域实体应在core/entities/中
5. 仓库接口应在core/repositories/中

## 代码生成

1. 使用build_runner进行代码生成：
   ```bash
   flutter pub run build_runner build
   ```
2. 为以下内容生成代码：
   - Freezed模型
   - Riverpod providers
   - Retrofit API客户端
   - JSON序列化
   - FlutterGen资源

## 测试指南

1. 将测试放在test目录中
2. 为业务逻辑编写单元测试
3. 为UI组件实现组件测试
4. 使用bloc_test进行BLoC测试

```dart
void main() {
  group('AccountBloc', () {
    late AccountBloc accountBloc;
    late MockAccountRepository mockRepository;

    setUp(() {
      mockRepository = MockAccountRepository();
      accountBloc = AccountBloc(mockRepository);
    });

    blocTest<AccountBloc, AccountState>(
      'emits [loading, loaded] when account is loaded successfully',
      build: () => accountBloc,
      act: (bloc) => bloc.add(const AccountLoaded()),
      expect: () => [
        const AccountState.loading(),
        AccountState.loaded(mockAccount),
      ],
    );
  });
}
```

## 数据流和模型转换

### 数据流向
1. **UI层**：用户交互触发Event
2. **BLoC层**：处理Event，调用Use Case
3. **Use Case层**：执行业务逻辑，调用Repository
4. **Repository层**：协调数据源，返回Domain实体
5. **数据源**：提供原始数据

### 模型转换策略
- **DTO (Data Transfer Objects)**：位于Infrastructure层，用于API通信
- **Entity**：位于Domain层，表示核心业务对象
- **UI Models**：位于Presentation层，针对UI展示优化

转换责任：
- Infrastructure层负责DTO与Entity的互相转换
- Presentation层负责Entity与UI Model的互相转换

示例：
```dart
// Infrastructure层
@JsonSerializable()
class UserDto {
  final String id;
  final String name;
  final String email;
  
  UserDto({required this.id, required this.name, required this.email});
  
  factory UserDto.fromJson(Map<String, dynamic> json) => _$UserDtoFromJson(json);
  Map<String, dynamic> toJson() => _$UserDtoToJson(this);
  
  // 转换为领域实体
  User toDomain() => User(id: id, name: name, email: email);
}

// Domain层
class User {
  final String id;
  final String name;
  final String email;
  
  User({required this.id, required this.name, required this.email});
}

// Presentation层
class UserViewModel {
  final String id;
  final String displayName;
  final String emailAddress;
  final bool isVerified;
  
  UserViewModel({
    required this.id, 
    required this.displayName, 
    required this.emailAddress,
    this.isVerified = false,
  });
  
  factory UserViewModel.fromEntity(User user) => UserViewModel(
    id: user.id,
    displayName: user.name,
    emailAddress: user.email,
  );
}
```

## 命名指南

1. 遵循一致的命名规范：

   - 目录名：snake_case
   - 文件名：snake_case
   - 类名：UpperCamelCase
   - 变量名：camelCase

2. 功能特定命名：

   - 页面：NounPage（例如，HomePage）
   - 组件：NounComponent（例如，UserCard）
   - 模型：NounModel（例如，UserModel）
   - Providers：NounProvider（例如，UserProvider）
   - 仓库：NounRepository（例如，UserRepository）

3. 一般原则：
   - 实体使用名词或形容词
   - 动作使用动词
   - 考虑命名的复数形式
   - 保持名称清晰和描述性

## 静态分析

1. 遵循pedantic_mono lint规则
2. 使用custom_lint进行额外检查
3. 使用riverpod_lint进行Riverpod特定规则检查
4. 修复所有分析警告
5. 使用适当的空安全模式
---
description: 基于 Riverpod 的特性优先架构
globs: *
alwaysApply: true
---
## 概述

本文档概述了基于实际项目分析的当前项目结构和架构模式。遵循这些指南以在应用Flutter最佳实践的同时保持一致性。

## 架构概述

- 基于BLoC模式的Feature-first架构
- 基于Hooks的组件组合
- 领域驱动设计(DDD)的分层架构
  - Domain层：业务逻辑和规则
  - Infrastructure层：数据访问和外部服务
  - Presentation层：用户界面和交互
- SharedPreferences用于本地持久化
- Retrofit用于API通信
- 使用flutter_lints和custom_lint进行静态分析

## 项目结构

```
lib/
  ├── features/           // 功能模块
  │   ├── account/       // 账户相关功能
  │   │   ├── domain/   // 领域层
  │   │   │   ├── entities/     // 领域实体
  │   │   │   ├── repositories/ // 仓库接口
  │   │   │   └── use_cases/    // 用例（业务逻辑）
  │   │   ├── infrastructure/   // 基础设施层
  │   │   │   └── repositories/ // 仓库实现
  │   │   └── presentation/     // 表现层
  │   │       ├── pages/       // 页面
  │   │       ├── widgets/     // 组件
  │   │       └── bloc/        // 状态管理。领域层（domain）应该保持 框架无关性，Bloc 的框架依赖性（如 Flutter 的 Stream/Bloc 库）使其更适合放在表现层
  │   │           ├── account_bloc.dart     // BLoC类
  │   │           ├── account_event.dart    // 事件定义
  │   │           └── account_state.dart    // 状态定义
  │   ├── authentication/      // 认证功能
  │   ├── home/              // 主页功能
  │   ├── setting/           // 设置功能
  │   └── ...               // 其他功能模块
  ├── core/                  // 核心功能
  │   └── di/              # 依赖注入配置
  │   │   ├── injector.dart
  │   │   └── modules/     # 按功能划分的注入模块
  │   ├── presentation/      // 核心表现层
  │   │   └── widgets/      // 共享组件
  │   │   └── bloc/         // 全局状态管理           
  │   │       ├── app_bloc.dart  // 应用级BLoC
  │   │       ├── app_event.dart // 应用级事件
  │   │       └── app_state.dart // 应用级状态
  │   ├── exceptions/        // 自定义异常
  │   ├── extensions/        // 扩展方法
  │   ├── utils/            // 工具函数
  │   ├── router/           // 导航路由
  │   ├── res/              // 资源（生成的）
  │   │   ├── colors/
  │   │   ├── strings/
  │   │   ├── themes/
  │   │   └── icons/
  │   └── custom_hooks/     // 自定义Flutter hooks
  └── test/                  # 测试目录与源码平行（可选）
  │   └── features/
  │   │   └── account/
  │   │       ├── domain/
  │   │       └── presentation/
  ├── app.dart              // 应用配置
  └── main.dart             // 入口点
```
## 领域驱动设计（DDD）层次说明

### Domain层
- 包含业务逻辑和规则
- 不依赖于具体的技术实现
- 包括：
  - Entities（实体）：核心业务对象
  - Repositories（仓库接口）：定义数据操作契约
  - Use Cases（用例）：具体业务逻辑实现

### Infrastructure层
- 实现Domain层定义的接口
- 处理外部系统交互
- 包括：
  - Repository实现
  - 外部服务适配器
  - 数据源实现

### Presentation层
- 处理用户界面和交互
- 使用Domain层的Use Cases
- 包括：
  - Pages（页面）
  - Widgets（组件）
  - View Models/Controllers
  

## 包依赖

### 核心依赖 🎯
- `flutter_sdk: ">=3.4.0"` - Flutter 框架核心
- `cupertino_icons` - iOS 风格图标

### 国际化
- `flutter_localizations` - 国际化支持
- `flutter_timezone` - 时区信息
- `timezone` - 时区数据解析与转换

### 状态管理和路由 🔄
- `flutter_bloc: ^8.1.4` - Bloc 状态管理
- `bloc` - Bloc 核心实现
- `bloc_concurrency` - Bloc 并发控制
- `flutter_hooks` - React 风格 Hooks
- `auto_route: ^10.0.1` - 路由管理库

### 数据管理 💾
- `objectbox` - 本地存储
- `retrofit` - API 客户端生成
- `dio` - HTTP 客户端

### 权限验证
- `permission_handler: ^11.4.0` - HTTP 客户端


### 日志
- `logging` - ​基础日志框架
- `logger` - 基于 logging 实现增强

### UI 组件 🎨

#### 图片

- `image_picker` - 图片选择
- `cached_network_image` - 图片缓存库
- `extended_image: ^10.0.0` - 官方 Image 的扩展组件
- `flutter_image_compress: ^2.4.0` - 压缩图片
- `image_cropper` - 图片裁剪
- `image_gallery_saver_plus: ^4.0.1` - ​保存到设备本地相册
- `flutter_svg` - SVG 支持

#### 交互

- `pull_to_refresh_flutter3` - 下拉刷新
- `numberpicker` - 数字选择器
- `flutter_slidable` - 滑动操作（实现列表项左右滑动显示操作面板（如删除、分享按钮））
- `adaptive_dialog` - 自适应对话框
- `page_transition` - 页面动画
- `flutter_inappwebview` - 内置浏览器
- `flutter_linkify` - 自动检测文本中的 URL 和电子邮件地址，转换为可点击的链接（常配合 url_launcher 使用，实现链接跳转功能）
- `url_launcher` - URL 处理
- `map_launcher` - 地图启动
- `flutter_local_notifications` - 本地通知
- `share_plus` - 调用平台原生分享功能
- `dismissible_page` - 提供类似 iOS 的页面滑动关闭效果，用户可通过左右滑动返回上一页（全屏弹窗、图片查看器等需要手势操作的界面。）


### 测试
- `bloc_test` - Bloc 测试
- `mockito` - 模拟数据

### 开发工具 🛠️

#### 代码规范
- `flutter_lints` - Flutter默认代码规范
- `custom_lint` - 自定义检查


#### 代码生成
- `freezed_annotation` - 不可变对象
- `json_serializable` - JSON 生成
- `json_annotation` - JSON 序列化
- `equatable` - 相等性比较
- `build_runner` - 代码生成
- `freezed` - 不可变类生成
- `retrofit_generator` - Retrofit 生成
- `flutter_gen_runner` - 资源生成


### 其他
- `rxdart` - 响应式编程库
- `uuid` - 生成符合 RFC4122 标准的全局唯一标识符（UUID），支持版本 1（时间戳）、4（随机数）和 5（命名空间哈希）
- `collection` - Dart 标准库的扩展，提供高级集合操作工具（如优先级队列、双向映射）
- `quiver` - 实用工具库，包含集合处理、异步工具、缓存管理等模块




## 状态管理指南

1. BLoC模式实现原则：
   - 使用Event作为输入
   - 使用State作为输出
   - 使用emit发送新状态
   - 实现clear separation of concerns

2. BLoC类实现规范：
```dart
class AccountBloc extends Bloc<AccountEvent, AccountState> {
  AccountBloc(this._accountRepository) : super(const AccountState.initial()) {
    on<AccountLoaded>(_onAccountLoaded);
    on<AccountUpdated>(_onAccountUpdated);
  }

  final AccountRepository _accountRepository;

  Future<void> _onAccountLoaded(
    AccountLoaded event,
    Emitter<AccountState> emit,
  ) async {
    emit(const AccountState.loading());
    try {
      final account = await _accountRepository.getAccount();
      emit(AccountState.loaded(account));
    } catch (error) {
      emit(AccountState.error(error.toString()));
    }
  }
}
```

3. Event定义规范：
```dart
abstract class AccountEvent extends Equatable {
  const AccountEvent();

  @override
  List<Object?> get props => [];
}

class AccountLoaded extends AccountEvent {
  const AccountLoaded();
}

class AccountUpdated extends AccountEvent {
  const AccountUpdated(this.account);

  final Account account;

  @override
  List<Object?> get props => [account];
}
```

4. State定义规范：
```dart
@freezed
class AccountState with _$AccountState {
  const factory AccountState.initial() = _Initial;
  const factory AccountState.loading() = _Loading;
  const factory AccountState.loaded(Account account) = _Loaded;
  const factory AccountState.error(String message) = _Error;
}
```

5. UI集成规范：
```dart
class AccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (context) => AccountBloc(
        context.read<AccountRepository>(),
      )..add(const AccountLoaded()),
      child: BlocBuilder<AccountBloc, AccountState>(
        builder: (context, state) {
          return state.when(
            initial: () => const SizedBox(),
            loading: () => const CircularProgressIndicator(),
            loaded: (account) => AccountView(account: account),
            error: (message) => ErrorView(message: message),
          );
        },
      ),
    );
  }
}
```

## 功能组织

1. 每个功能应该在features/目录下自包含
2. 功能结构应包括：
   - pages/（UI界面）
   - widgets/（功能特定组件）
   - bloc/（状态管理）
   - repositories/（数据访问）
   - entities/（功能特定实体）

## 核心层指南

1. 核心功能应放置在适当的core/子目录中
2. 共享组件应放置在core/widgets/中
3. 业务逻辑应在core/use_cases/中
4. 领域实体应在core/entities/中
5. 仓库接口应在core/repositories/中

## 代码生成

1. 使用build_runner进行代码生成：
   ```bash
   flutter pub run build_runner build
   ```
2. 为以下内容生成代码：
   - Freezed模型
   - Riverpod providers
   - Retrofit API客户端
   - JSON序列化
   - FlutterGen资源

## 测试指南

1. 将测试放在test目录中
2. 为业务逻辑编写单元测试
3. 为UI组件实现组件测试
4. 使用bloc_test进行BLoC测试

```dart
void main() {
  group('AccountBloc', () {
    late AccountBloc accountBloc;
    late MockAccountRepository mockRepository;

    setUp(() {
      mockRepository = MockAccountRepository();
      accountBloc = AccountBloc(mockRepository);
    });

    blocTest<AccountBloc, AccountState>(
      'emits [loading, loaded] when account is loaded successfully',
      build: () => accountBloc,
      act: (bloc) => bloc.add(const AccountLoaded()),
      expect: () => [
        const AccountState.loading(),
        AccountState.loaded(mockAccount),
      ],
    );
  });
}
```

## 性能指南

1. 使用extended_image实现适当的图片缓存
2. 使用pull_to_refresh_flutter3优化列表视图
3. 遵循适当的状态管理模式
4. 实现适当的Firebase缓存策略

## 命名指南

1. 遵循一致的命名规范：

   - 目录名：snake_case
   - 文件名：snake_case
   - 类名：UpperCamelCase
   - 变量名：camelCase

2. 功能特定命名：

   - 页面：NounPage（例如，HomePage）
   - 组件：NounComponent（例如，UserCard）
   - 模型：NounModel（例如，UserModel）
   - Providers：NounProvider（例如，UserProvider）
   - 仓库：NounRepository（例如，UserRepository）

3. 一般原则：
   - 实体使用名词或形容词
   - 动作使用动词
   - 考虑命名的复数形式
   - 保持名称清晰和描述性

## 静态分析

1. 遵循pedantic_mono lint规则
2. 使用custom_lint进行额外检查
3. 使用riverpod_lint进行Riverpod特定规则检查
4. 修复所有分析警告
5. 使用适当的空安全模式